# Generated by Cargo / Tauri（target 已上移到 workspace 根，见仓库根 .gitignore）
/gen/schemas
# 打包期从 engram-mcp crate 拷贝的 MCP server exe（构建产物，不入库）
/resources/*
# 但保留 models 占位目录入库：tauri resources 列表引用了 resources/models，
# 目录缺失会让 tauri-build 在干净 checkout（CI）上失败；模型本体仍不入库（打包脚本本地拷贝）
!/resources/models/
/resources/models/*
!/resources/models/.gitkeep
