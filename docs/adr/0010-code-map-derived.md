# ADR 0010-code-map-derived

- **状态**：已接受（2026-09-07）
- **背景**：背景：静态手写骨架必然与代码漂移，漂移的骨架比没有更危险。
- **决定**：决定：tree-sitter 自动提取（公开接口优先）+ 变更 stale 联动 + AI 只做加法；挂派生区 code_map/。
- **后果**：后果：骨架永远可重生成；AI 进场成本 O(骨架)；需要 tree-sitter 依赖与提取后端。
