# ADR 0001-file-as-truth

- **状态**：已接受（2026-09-07）
- **背景**：背景：需要一种不会腐化的记忆载体；AI 与 GUI 并行写入同一工程记忆。
- **决定**：决定：节点 .md 文件（YAML 头+自由正文）为唯一事实源；关系叙述读取时动态拼装不回写。
- **后果**：后果：改一条边零同步成本；数据永远人可读可 git；代价是所有检索/统计必须走派生层（见 0004）。
