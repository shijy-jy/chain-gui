# M8 自验收报告 — 校验状态面板 + 初始化向导

code_baseline: BL-20260813-02
milestone: M8
ai: ai-frontend（Codely CLI Claude Sonnet 4.5）
date: 2026-08-13

## 任务清单完成度（7 步）

- [x] Step 0 precheck（M5 入库确认 + cargo test 37 passed + vite build 0 error + svelte-check 0/0 + CURRENT.md status=idle）
- [x] Step 1 后端 init_chain command（commands/init_chain.rs + lib.rs 注册 + scanner::frontmatter::now_iso8601 复用）
- [x] Step 2 底部状态条组件（src/components/StatusBar.svelte：32px + #111 + 节点/边数 + 校验状态色点）
- [x] Step 3 校验详情抽屉（状态条上方 ~200px 可滚动面板，errors 在前 warnings 在后，色点 + 等宽字体）
- [x] Step 4 工具栏"重新扫描"按钮（工具栏右侧胶囊按钮，无目录时禁用）
- [x] Step 5 初始化向导（needsInit 分支："该目录还不是 chain 工程" + [初始化 chain] → invoke init_chain）
- [x] Step 6 后端单测（test_init_chain_creates_structure + test_init_chain_idempotent，cargo test 39/39 全过）
- [x] Step 7 自测 + 收尾（vite build 0 error + svelte-check 0 errors 0 warnings + 零新依赖）

## 5 硬指标

1. **npx vite build** → 0 error（119 modules transformed, built in 3.62s）
2. **npx svelte-check** → 0 errors 0 warnings
3. **cargo test** → 39 passed; 0 failed（M5 基线 37 + init_chain 2 新增 = 39）
4. **零新依赖**（Cargo.toml / package.json 均未改动）
5. **init_chain 幂等**（已有 g-001.md 时不覆盖，单测验证）

## 交付物

- **前端**：`src/components/StatusBar.svelte`（状态条 + 抽屉）+ `src/App.svelte` 改动（状态条挂载 / 向导分支 / 重新扫描按钮）
- **后端**：`src-tauri/src/commands/init_chain.rs`（init_chain command + 2 单测）+ `lib.rs` 注册 + `commands/mod.rs` mod 声明
- **self_review**：本文件

## git log 输出

```
e0c976b BL-20260813-02: feat: M8 校验状态面板 + 初始化向导 + init_chain command + 2 单测
cf2adfa chore: BL-20260813-02 M5验收通过（reviews/M5.md + 任务卡/台账翻状态），M8就绪
29a50ea BL-20260813-02: done: M5 文件监听 + 自动重载完成 + self_review
```

## 已知问题 / 留给后续

- **交互验收未完整**：手动链路（选 test-data 看绿点 / 改非法值看红点抽屉 / 选空目录看向导 / 初始化 chain）需瑾瑜在 dev 窗口手动操作确认。代码逻辑已实现。
- **状态条挤压画布高度**：App 已是 flex column 布局，画布 flex:1 自适应。状态条 32px + 抽屉 ~200px 展开时画布会缩小，cytoscape resize fit 在 M4 已有 onResize 逻辑可复用。
- **向导特征串匹配**：用 `msg.includes('不存在 .chain')` 匹配 scan_chain 的错误消息。如后端改错误文案需同步更新。

## 交接给 coze

请 coze 验收后：
1. 在 ai_workspace/ai-coordinator/reviews/M8.md 写验收记录
2. 通知瑾瑜手动验视觉（选目录看状态条 / 改非法值看抽屉 / 选空目录看向导）
3. M8 验收通过后决定下一步（M9 打包？）

—— ai-frontend
