# M8 验收报告 — 校验状态面板 + 初始化向导

code_baseline: BL-20260813-02
milestone: M8
执行: ai-frontend（Codely CLI Claude Sonnet 4.5）
验收: coze（怀瑾）
date: 2026-08-13

## 一、任务卡核对（7 步 + 5 硬指标）

| 步骤 | 结果 | 证据 |
|------|------|------|
| Step 0 precheck | ✅ | M5 已入库（cf2adfa）+ 基线测试 37 全过才动工 |
| Step 1 init_chain command | ✅ | commands/init_chain.rs + mod.rs/lib.rs 注册 |
| Step 2 StatusBar.svelte | ✅ | 32px 底条 + #111 + 节点/边数 + 绿/红/黄 8px 色点 |
| Step 3 校验详情抽屉 | ✅ | max-height 200px 滚动面板，errors 在前 warnings 在后，等宽字体 |
| Step 4 重新扫描按钮 | ✅ | 工具栏右侧胶囊按钮（loading/无 snapshot 禁用）+ 状态条内重扫入口 |
| Step 5 初始化向导 | ✅ | needsInit 特征串分支 + init-btn → invoke init_chain，防重入闸 |
| Step 6 后端单测 | ✅ | creates_structure + idempotent 2 条，实测 39 passed |

硬指标：vite build 0 error ✅ / svelte-check 0/0 ✅ / cargo test 39 ✅ / 零新依赖 ✅ / init_chain 幂等 ✅

## 二、代码审查（全量通读）

- **init_chain.rs（71 行）**：create_dir_all 建 .chain/nodes/ → g-001.md 存在则跳过（幂等核心）→ now_iso8601() 复用 → scan_chain_dir 重扫返回 snapshot。与任务卡参考实现一致，错误信息中文可读。
- **单测 2 条**：creates_structure 断言文件存在 + snapshot 1 节点 + validation.valid；idempotent 先 init 再手改 title 再 init，断言"我改过的"未被覆盖——幂等验证方式扎实。
- **StatusBar.svelte（178 行）**：Svelte 5 全合规（$props 注解式 / $state / $derived / onclick 属性式，未踩 M6 泛型坑）；toggleDrawer 仅 hasIssues 可切；无目录显示"未选择目录"；视觉沿用 M6（#111/8px 色点 #34d399 #f87171 #fbbf24/胶囊按钮/无 emoji）。
- **App.svelte diff**：loadChain 捕获 `不存在 .chain` 特征串 → needsInit；handleInit 带 initializing 防重入；header 旧 stat 删除（节点/边数挪状态条，设计归位）；`<StatusBar snapshot={snapshot} onrescan={loadChain} />`——M5 listener 替换 snapshot 后状态条经 $derived 自动刷新，零额外接线（印证 D-45 预判）。
- **已知风险确认**：向导依赖后端错误文案特征串匹配，后端改文案需同步（已入 self_review 遗留，可接受）。

## 三、实跑实证（coze 本机实跑，非转述）

```
cargo test --lib     → 39 passed; 0 failed（0.07s）
npx vite build       → built in 3.27s，0 error（498.61 kB / gzip 162.84 kB）
npx svelte-check     → 0 errors and 0 warnings
```

## 四、git 实证

```
e6f1b51  BL-20260813-02: done: M8 + self_review
e0c976b  BL-20260813-02: feat: M8（状态面板 + 向导 + init_chain + 2 单测）
cf2adfa  chore: M5验收通过（前序基线）
```

diff cf2adfa..e0c976b 指定路径实证：**package.json / Cargo.toml 零改动**（零新依赖硬指标直接实证）；改动文件全部在任务卡范围内（init_chain.rs 新增 / mod.rs +1 / lib.rs +1 / App.svelte / StatusBar.svelte 新增）。

## 五、手动链路等效说明

按 D-42 第 3 条，以下手动链路留开发者一次 `cargo tauri dev` 顺手确认，等效理由：

1. **状态条显示**（绿点/节点边数）：数据通路 = snapshot.validation + manifest，该结构 M7 已被 18 条测试矩阵锁定，前端仅取值渲染，svelte-check 0/0 保证绑定正确。
2. **非法值 → 红点 → 抽屉**：errors 数组由 M7 schema 校验产出（已有测试覆盖），抽屉为纯渲染逻辑。
3. **空目录 → 向导 → 初始化**：init_chain 正确性与幂等已被 2 条新单测覆盖（TempDir 真实文件系统验证）；前端分支仅特征串判断 + invoke。

端到端视觉/交互链路（色点观感、抽屉展开手感、向导按钮流程）留开发者实跑确认，不阻塞验收结论。

## 六、结论

**✅ M8 验收通过。** 7 步全完成、5 硬指标实测全绿、零新依赖、git 合规、代码零 unwrap 零告警。v1.0 全部功能里程碑（M1-M8）至此闭环，剩 M9 打包。

遗留（不阻塞）：
- 手动链路 × 3（状态条观感 / 非法值抽屉 / 向导初始化）+ M5 watcher 手动链路 + M4 写回链路，可一次 dev 合并确认。
- 向导特征串耦合后端错误文案（改文案需同步前端）。

—— coze（怀瑾）
