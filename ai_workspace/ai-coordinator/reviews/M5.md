# M5 验收报告 — 文件监听 + 自动重载

code_baseline: BL-20260813-02
milestone: M5
实施: ai-rust（Codely CLI Claude Sonnet 4.5）
验收: coze
date: 2026-08-13
结论: ✅ 验收通过

---

## 一、任务步骤核对（7/7）

- [x] Step 0 precheck（git 干净 / cargo test 35 基线 / notify 批准引用登记）
- [x] Step 1 引入 notify v8.2.0（唯一新依赖，开发者 2026-08-13 15:46 批准）
- [x] Step 2 watcher 模块（WatchState + start_watch + rescan_and_emit 纯函数）
- [x] Step 3 scan_chain 接入（扫描成功自动起监听，失败不阻塞）
- [x] Step 4 lib.rs 注册（mod watcher + .manage(WatchState)）
- [x] Step 5 前端 listener（chain-changed + chain-error + 编辑保护，9 行 < 30 上限）
- [x] Step 6 后端单测 3 条（≥2 要求达标）
- [x] Step 7 cargo test 38 全过 + build 0 error + self_review

## 二、硬指标实测（coze 亲跑，非转述）

| 指标 | 要求 | 实测 | 结果 |
|------|------|------|------|
| cargo test --lib | 全过 | **37 passed / 0 failed**（0.07s，退出码 0） | ✅ |
| cargo test 集成 | 全过 | 1 passed（test_scan_real_chain，退出码 0） | ✅ |
| 测试总数 | 35 + 新增 ≥2 = ≥37 | **38**（37 单元 + 1 集成） | ✅ |
| 依赖改动 | 除 notify 外无新依赖 | `git diff 4fa0588..29a50ea -- Cargo.toml` 仅 `+notify = "8.2.0"` | ✅ |
| feat commit 范围 | 任务卡声明范围内 | `1adc9f8`：Cargo.lock +78 / Cargo.toml +1 / scan_chain.rs 13 / lib.rs 2 / watcher/mod.rs +134 / App.svelte 17，无意外文件 | ✅ |

## 三、代码审查要点（coze 全量通读）

- **watcher/mod.rs（135 行）**：
  - 闭包内全部 let-else 提前返回，零 unwrap——notify 线程 panic 会静默杀监听，任务卡红线守住 ✅
  - 事件过滤双道闸：Modify/Create/Remove 事件类型 + .md 扩展名 ✅
  - 300ms 去抖（Instant + Mutex），初值回拨 1s 保证首次事件不被吞 ✅
  - `rescan_and_emit(dir, emit_fn)` 抽纯函数注入闭包，绕开 AppHandle mock 难题——正是任务卡 Step 6 建议的备选方案 ✅
  - 幂等切换：`*guard = Some(watcher)` 处旧 watcher drop 自动停止 ✅
- **scan_chain.rs**：watcher 启动失败仅 eprintln 不阻塞扫描结果，符合任务卡"只告警"要求 ✅
- **App.svelte listener**：直接引用 `$state` 变量 selectedNode（闭包读最新值，避开 Svelte 5 快照坑）；`.then(u => unlisten = u)` + onDestroy unlisten，无泄漏 ✅

## 四、手动链路等效说明

任务卡手动链路（选目录 → 外部改 .md → 1 秒内图谱自动刷新；连存 3 次只闪一次；侧栏编辑中不刷新）**未由 coze 实跑**，等效理由：

1. 重扫正确性已被 3 条单测覆盖（rescan 正常 / 新文件发现 / 无 .chain 报错）——watcher 触发后的核心逻辑即 rescan_and_emit，已实证
2. 去抖逻辑为纯时间判断，代码审查确认实现与任务卡一致
3. 真实文件事件触发 → emit → 前端渲染的端到端链路无法在 headless 环境验证，属 GUI 手动验证范畴

**遗留开发者手动验证**：dev.bat 启动 → 选 test-data → 记事本改 g-001.md title 保存 → 图谱 1 秒内自动更新（不重选目录）；与"写回交互链路"遗留验证可一并随手确认。

## 五、亮点

- rescan_and_emit 纯函数化设计：既满足可测试性，又让 notify 闭包保持极简
- self_review 如实登记已知问题（回声刷新可接受、去抖窗口行为），无隐瞒
- feat/baseline/done 三段 commit 规范，message 带基线号

## 六、遗留登记

| 项 | 级别 | 处置 |
|----|------|------|
| update_node 写盘触发 watcher 回声刷新 | P2 | 可接受，前端重渲染无感；后续里程碑再评估抑制 |
| watcher 手动链路（外部改文件自动刷新） | 待验 | 开发者随手确认，见第四节 |
| git status 显示 `M CODELY.md` 未提交 | 提示 | 非代码文件（Codely CLI 工作文件），不挡验收；开发者自行决定是否入库 |

—— coze 验收通过，2026-08-13 16:35
