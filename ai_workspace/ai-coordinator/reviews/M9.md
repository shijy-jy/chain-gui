# M9 验收报告 —— Windows 打包发布

> 验收人：coze（怀瑾）
> 日期：2026-08-13
> 基线：BL-20260813-02
> 结论：✅ 验收通过（实装链路留开发者）

---

## 一、验收范围

M9 任务卡（task_cards/M9_package.md）4 步：precheck → tauri.conf.json 修改 → 构建 → 产物核验。本任务由 coze 主导直改，无协作 AI。

## 二、配置变更（git diff 实证）

`src-tauri/tauri.conf.json` 共 5 处改动，零新依赖（Cargo.toml / package.json 未动）：

| 字段 | 原值 | 现值 |
|------|------|------|
| `identifier` | `com.tauri.dev`（脚手架占位） | `com.chaingui.desktop` |
| `version` | `0.1.0` | `1.0.0` |
| `bundle.targets` | `"all"` | `["nsis"]` |
| `app.windows[0].devtools` | `true` | `false` |

identifier 中途由 `com.chaingui.app` 改定为 `com.chaingui.desktop`：tauri build 警告 `.app` 后缀与 macOS bundle 扩展冲突；Windows 虽无影响，但 identifier 是 NSIS 安装/升级识别凭据，定稿后不再变动，保 v1.1+ 升级链连续。

## 三、构建过程实录

| 次数 | 时间 | 结果 |
|------|------|------|
| 1 | 17:40 | ✅ 成功（release 2m51s + NSIS 下载/解压/打包全通），产旧 identifier 版 setup.exe |
| 2 | 17:41 | ❌ NSIS 阶段 `os error 145 目录不是空的`（tauri-cli 2.11.4 重复 build 撞已有 NSIS 缓存目录的已知问题；产物未被破坏） |
| 3 | 17:44 | ❌ 清缓存后 release 45.09s 成功，NSIS 重下载 `timeout: global`（GitHub 网络超时） |
| 4 | 17:46 | ❌ 同上，`io: Connection refused` |
| 5 | 17:51 | ✅ 成功（网络探测通过 → 下载/解压/makensis 全通），产正式版 setup.exe |

教训固化：tauri-cli 2.x 重复 build 需先清 `%LOCALAPPDATA%\tauri\NSIS`；NSIS 工具链走 GitHub 下载，网络波动时先探测连通性再重试，不盲跑。

## 四、硬指标核验

1. `cargo tauri build` → 0 error 出包 ✅（`Finished 1 bundle at: ...\nsis\chain-gui_1.0.0_x64-setup.exe`）
2. NSIS 安装包真实存在 ✅（ls 实证：2,261,999 字节 ≈ 2.2MB，在线安装器含 WebView2 bootstrapper，尺寸合理）
3. 零新依赖 ✅（git status 实证：仅 tauri.conf.json 修改，Cargo.toml / package.json 零改动）
4. 构建后 `cargo test --lib` 39 passed 0 failed ✅（17:42 实跑 0.11s；17:51 出包后复核见 CURRENT 记录）

## 五、手动链路（等效说明与遗留）

M9 核心产物是安装包本身，**实装验证无法由 coze 等效替代**，留开发者：

1. 双击 setup.exe 安装 → 启动 → 选 test-data → 图谱/状态条/侧栏全功能顺一遍
2. 顺带一次清净 4 条存量手动链路：M4 写回交互 / M5 watcher 自动刷新 / M8 非法值红点抽屉 / 空目录向导初始化

等效理由：构建链全部硬指标已由实跑输出实证（编译 0 error、产物字节级存在、单测回归全绿），剩余仅为"安装后运行"的人工确认。

## 六、结论

**M9 验收通过。** v1.0 全部里程碑（Init + M1-M9）闭环，chain-gui 正式具备 Windows 分发形态。遗留：CODELY.md 未提交改动开发者自行处置；cytoscape-dagre 死依赖待 npm uninstall；v1.1 候选（自定义图标 / 向导特征串解耦 / 回声抑制）。

—— coze（怀瑾）2026-08-13
