# M7 验收报告 — schema 严格校验

code_baseline: BL-20260813-02
milestone: M7
实施: ai-rust（Codely CLI Claude Sonnet 4.5）
验收: coze
date: 2026-08-13
结论: ✅ **验收通过**

---

## 一、任务卡步骤核对（6/6）

- [x] Step 0 precheck（git 干净基线开工，commit `97837d2` baseline start）
- [x] Step 1 字段级校验 9 条规则
- [x] Step 2 结构级校验 5 条规则
- [x] Step 3 测试矩阵 18 条（要求 ≥10）
- [x] Step 4 集成 scan_chain + cargo test 全过
- [x] Step 5 self_review + commit（`ae81834` feat / `f3f3e56` done）

git 链核验：`97837d2` baseline → `ae81834` feat → `f3f3e56` done + self_review，工作区干净。

## 二、硬指标实测（coze 独立实跑，非转述 self_review）

| 指标 | 要求 | 实测 | 结果 |
|------|------|------|------|
| cargo test | 全过（含 ≥10 新测试） | **34 lib + 1 集成 = 35 全过，0 失败**（0.06s） | ✅ |
| cargo build | 0 error 0 warning | 0 error；链接器 .lib/.exp 通知为 MSVC 工具链固有噪音（M4 验收同口径放行） | ✅ |
| 新依赖 | 无 | 代码全手写实现（id 正则/RFC3339/civil 算法均无 crate）；Cargo.toml diff 另附 git 实证 | ✅ |
| 错误格式 | `[文件] 字段: 原因` | 实际报错带具体值，如 `[g-001.md] status: 非法枚举值 'done'` | ✅ |

## 三、代码审查要点（全文件通读）

**validator.rs（583 行）**：
- 字段级 9 条全覆盖，另**超额实现** parent 字段类型检查（任务卡未要求）
- id 缺失时 early return，避免级联误报——好设计
- 环检测 DFS + 排序去重，同一环只报一次
- root 唯一：0 个 / 多个根都报错，空目录合理跳过
- parent 类型约束定为 warning 级，与任务卡"待对口径后定级"一致
- `is_valid_rfc3339` 固定位置 + 值域双层校验；时区偏移换算 UTC 的 sign 逻辑核对无误
- `days_from_civil` 标准 Hinnant 算法

**walker.rs 容错集成（任务卡外的正确加固）**：
- 读取失败 / frontmatter 解析失败 / 反序列化失败 → 全部进 validation.errors 继续扫描，不再整目录 bail
- 字段级校验在 Mapping 层做（反序列化失败也能报字段错误）

**测试矩阵 18 条**：1 全合法 + 9 字段级违法 + 4 结构级违法 + 1 warning + 3 辅助函数。每条断言错误文案关键词，不是只数数量。

## 四、手动链路等效说明

1. **"改坏 g-001 status=done 能精确报错"**：由 `test_invalid_status_enum` 单测等效覆盖（测试正是用 replace 注入 `status: done` 后断言 `status: 非法枚举值`），且 walker 集成路径直调 validator。不再手动污染 test-data。
2. **"现有 test-data 必须全合法"**：coze 人工逐文件核对 5 节点（g-001/d-001/d-002/t-001/v-001）——字段级 9 条 + 结构级 5 条全部合法，无 error 无 warning。✅

## 五、亮点与顺带修复

- **ai-rust 顺带修复了 M4 遗留的测试期望值错误**：`civil_from_days(2000-02-29)` 正确值 11016（M4 时误写 10957，即 2000-01-01 的值）。M4 的"17 全过"当时只是预期未实跑，此次实跑 35 全过含此修正。
- 验收还确认了 M4 遗留验证项"cargo test 全过待实跑"——现已实跑闭环。

## 六、遗留（不挡验收）

- `validate()` 组合函数当前无调用方（walker 直调两个子函数），后续可删或留作公共 API
- validation 的 GUI 展示归 M8 状态面板
- 链接器 .lib/.exp 通知不可消除（MSVC 固有）

---

验收人：coze
