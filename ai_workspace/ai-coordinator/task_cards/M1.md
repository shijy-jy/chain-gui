---
AIGC:
    Label: "1"
    ContentProducer: 001191110102MACQD9K64018705
    ProduceID: 3436392644363562_0/project_7673108688902668594-files/task_cards/M1.md
    ReservedCode1: ""
    ContentPropagator: 001191110102MACQD9K64028705
    PropagateID: 3436392644363562#1786574435620
    ReservedCode2: ""
---
# M1 任务卡 — 项目脚手架

code_baseline: BL-20260812-01
target_ai: ai-rust
created: 2026-08-13 06:30
estimated: 3-4 人天
type: milestone-task
inherits: render_unified ai_workspace protocol (BL-20260811-01)
spec_ref: /Coze/Drive/怀瑾的新项目（4）/chain_protocol_gui_impl_plan_v3.html §M1 §6.4

---

## 目标

在 Init 任务卡已完成（git init + ai_workspace 协议就绪）的基础上，跑通 Tauri 2.x 空工程，dev 模式能开窗。

## 前置

- **必须**：Init 任务卡已验收通过——`G:\test1.x\.git/`、`ai_workspace/`、CODE_STATE.md、四个 AI 文件夹的 CURRENT.md 都已就绪。
- M1 验证 Init 状态后启动（见 Step 0）。

## 工程根目录

**`G:\test1.x\`**（已由用户创建，由 Init 任务卡完成协议初始化）

## 任务清单

按顺序执行，每完成一步按需 commit 一次（init / tauri init / 配置文件 / 自验收 都是天然的 commit 节点）。

### Step 0：Init 状态确认（precheck，2 分钟）

**不要跳过这步**。在装环境之前确认 Init 任务卡已完成，避免重复劳动或环境冲突。

打开 PowerShell 执行：

```bash
cd G:\test1.x

# 1. 目录里有 .git/ 和 ai_workspace/？
ls -Force
# 期望看到：.git/  .gitignore  ai_workspace/

# 2. git 仓库已初始化？
git log --oneline
# 期望看到 1 条 commit：BL-20260812-01: chore: Init chain-gui 工程 + ai_workspace 协作协议

# 3. ai_workspace/ 完整？
ls ai_workspace
# 期望看到：CODE_STATE.md  README.md  ai-coordinator  ai-frontend  ai-qa  ai-rust

# 4. 四个 AI 文件夹的 CURRENT.md 都在？
ls ai_workspace/ai-coordinator/CURRENT.md
ls ai_workspace/ai-rust/CURRENT.md
ls ai_workspace/ai-frontend/CURRENT.md
ls ai_workspace/ai-qa/CURRENT.md
# 四个文件都应存在

# 5. 工具配置（如果 Init 没配）
git config user.name
git config user.email
# 期望：ai-rust / ai-rust@chain-gui.local（如未设则 git config user.name "ai-rust"）
```

**如果 1-4 任何一步不符合**——不要继续，回到 Init 任务卡查漏，验收通过后再启动 M1。
**全部通过**——继续 Step 1。

### Step 1：安装 Rust / Node / Tauri CLI

按 v3 §5.1 "技术栈版本"：

```bash
# Rust 1.78+（Tauri 2 要求）—— 用 rustup 安装
# 下载 https://rustup.rs/ 运行，或：
winget install Rustlang.Rustup
rustup install stable
rustup default stable

# 验证
rustc --version
# 期望：rustc 1.78.0 或更高
cargo --version
# 期望：cargo 1.78.0 或更高
```

```bash
# Node 20+ —— 直接装 LTS
# 下载 https://nodejs.org/ 选 LTS，或：
winget install OpenJS.NodeJS.LTS

# 验证
node --version
# 期望：v20.x 或更高
npm --version
# 期望：10.x 或更高
```

```bash
# Tauri CLI 2.x
cargo install tauri-cli --version "^2.0"

# 验证
cargo tauri --version
# 期望：tauri-cli 2.x
```

**预期耗时**：Rust 工具链安装 5-15 分钟（首次下载 + 编译）；Node 几分钟；Tauri CLI 单独编译 3-5 分钟。**整个 Step 1 约 15-30 分钟**，让终端跑，别中断。

**如果装到一半报错**：
- 缺 MSVC build tools（Windows）→ 装 Visual Studio Build Tools 2022（C++ workload + Windows SDK）
- 网络问题导致 cargo / npm 拉包失败 → 换镜像源（USTC / 清华），或在 `~/.cargo/config.toml` 配 proxy
- 权限问题 → 用管理员 PowerShell（仅装工具链时需要，写代码不需要）

**完成后**：

```bash
cd G:\test1.x
git status
# 期望：干净（没有未提交改动）
# 如果有，commit 一下（理论上 Init 都已提交，不应该有）
```

**注意**：这一步**不做 commit**（没改任何工程文件，只装工具）。

### Step 2：初始化 Tauri 工程

```bash
cd G:\test1.x

# 用 Tauri CLI 直接 init（避免 npm create 模板污染）
cargo tauri init \
  --app-name "chain-gui" \
  --window-title "Chain Protocol GUI" \
  --frontend-dist "../dist" \
  --dev-url "http://localhost:1420" \
  --before-dev-command "npm run dev" \
  --before-build-command "npm run build"
```

> **如果 `cargo tauri init` 报"目录已存在"或要求空目录**，fallback 用 create-tauri-app：
>
> ```bash
> # 注意：create-tauri-app 要在空目录里跑，所以临时把 ai_workspace 移走
> cd G:\test1.x
> mv ai_workspace ai_workspace.bak
> cd G:\test1.x\..
> npm create tauri-app@latest test1.x -- --template svelte-ts
> # 然后把 ai_workspace.bak 移回去
> mv G:\test1.x\ai_workspace.bak G:\test1.x\ai_workspace
> ```
>
> 两种方式二选一，**不要再发明第三种**。建议优先 `cargo tauri init`（更轻），如失败再 fallback。

**这一步会创建**：
- `src-tauri/`（Rust 后端，含 Cargo.toml、tauri.conf.json、main.rs、icons/）
- `src/`（Svelte 前端，含 index.html、main.ts、App.svelte、package.json）
- `package.json`（根）
- 修改 `.gitignore`（自动加 src-tauri/target/、node_modules/ 等）

**验证**：
```bash
ls G:\test1.x
# 期望：.git/  .gitignore  ai_workspace/  package.json  src/  src-tauri/

ls G:\test1.x\src-tauri
# 期望：Cargo.toml  tauri.conf.json  src/  icons/  build.rs

ls G:\test1.x\src
# 期望：index.html  main.ts  App.svelte  vite-env.d.ts
```

**提交**：
```bash
cd G:\test1.x
git add -A
git commit -m "BL-20260812-01: feat: Tauri 2.x + Svelte 5 工程脚手架"
```

### Step 3：配置 tauri.conf.json + 写根 README + 加代码风格配置

#### 3.1 修改 `src-tauri/tauri.conf.json` 窗口默认值

把 `app.windows[0]` 改成：

```json
{
  "title": "Chain Protocol GUI",
  "width": 1280,
  "height": 800,
  "minWidth": 800,
  "minHeight": 600,
  "resizable": true,
  "fullscreen": false
}
```

#### 3.2 写根 `README.md`

`G:\test1.x\README.md`：

```markdown
# chain-gui

跨平台桌面应用：chain protocol 可视化与编辑工具。

- **后端**：Rust + Tauri 2
- **前端**：Svelte 5 + TypeScript
- **图谱**：cytoscape.js
- **AI 协作**：详见 `ai_workspace/README.md`

## 开发

\`\`\`bash
cargo tauri dev
\`\`\`

## 打包

\`\`\`bash
cargo tauri build
\`\`\`

## 目录

- `src-tauri/`：Rust 后端
- `src/`：Svelte 前端
- `ai_workspace/`：AI 协作区

## 协议

- 协作协议：见 `ai_workspace/README.md`
- 规划书：见项目空间 `chain_protocol_gui_impl_plan_v3.html`
- 基线：BL-20260812-01（每完成一个里程碑 BUMP）
```

#### 3.3 加代码风格配置

`.editorconfig`（根目录）：

```ini
root = true

[*]
indent_style = space
indent_size = 2
end_of_line = lf
charset = utf-8
trim_trailing_whitespace = true
insert_final_newline = true

[*.rs]
indent_size = 4

[*.{md,yml,yaml}]
trim_trailing_whitespace = false
```

`rustfmt.toml`（根目录）：

```toml
edition = "2021"
max_width = 100
tab_spaces = 4
```

`.prettierrc`（根目录）：

```json
{
  "semi": true,
  "singleQuote": true,
  "trailingComma": "es5",
  "printWidth": 100,
  "tabWidth": 2
}
```

`.gitignore`（根目录，**追加**到 Init 已建的内容）：

```gitignore
# Build outputs（已由 Tauri init 自动加，确认存在）
src-tauri/target/
dist/
node_modules/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# AI 临时产物
*.log
*.tmp

# 不要忽略 ai_workspace 里的 .gitkeep
!ai_workspace/**/*.gitkeep
```

**提交**：
```bash
cd G:\test1.x
git add -A
git commit -m "BL-20260812-01: chore: tauri.conf 窗口配置 + 根 README + 代码风格配置"
```

### Step 4：验证 dev 模式开窗

```bash
cd G:\test1.x
cargo tauri dev
```

**预期**：
- 终端输出 Vite/Webpack 编译 → Rust 编译 → "App listening on http://localhost:1420"
- 弹出窗口，标题 "Chain Protocol GUI"，内容是 Tauri 默认欢迎页（Svelte 模板）
- 窗口可关闭、可调整大小

**如果报错**：
- 缺 `wasm32-unknown-unknown` target → `rustup target add wasm32-unknown-unknown`（一般 Tauri 不需要）
- Webview2 加载失败（Windows）→ 装 Webview2 Runtime（Win11 自带，Win10 缺）
- 端口冲突 → 改 `tauri.conf.json` 的 devUrl
- 编译失败 → 看终端报错，先确认 Rust 版本和 Visual Studio Build Tools 都齐

**如果 dev 模式能跑通**：

- 截图保存：`ai_workspace/ai-rust/screenshots/M1/dev-window.png`
- 终端输出保存：`ai_workspace/ai-rust/screenshots/M1/dev-output.txt`（可选）

**关闭 dev 模式**：在终端按 Ctrl+C 终止。

### Step 5：done commit + 基线 BUMP

```bash
# 1. 更新 CODE_STATE.md
# 把 "进行中" 那条改成完成，新增到 "已完成"
# 改 code_baseline: BL-20260812-01 → BL-20260812-02
# 改 last_update: <今天>
# 新增基线历史行 BL-20260812-02
```

**CODE_STATE.md 更新后长这样**（关键改动）：

```markdown
# CODE_STATE — chain-gui

code_baseline: BL-20260812-02
last_update: 2026-MM-DD
current_status: M1 done, M0 待启动

## 基线历史
| 基线号 | 日期 | 状态 | 主导 AI | 摘要 |
|--------|------|------|---------|------|
| BL-20260812-01 | 2026-08-13 | active | ai-coordinator | 初始基线，v3 规划就绪，Init 启动 |
| BL-20260812-02 | 2026-MM-DD | active | ai-rust | M1 Tauri 2 + Svelte 5 脚手架完成 |

## 进行中
（暂无 — M1 完成，等 coze 启动 M0）

## 已完成
- Init 工程初始化 — 主导: ai-rust — 完成于 2026-08-13
- M1 项目脚手架 — 主导: ai-rust — 完成于 2026-MM-DD

## 阻塞
（暂无）

## 已加入 AI 名单
| AI 角色 | 工具 | 版本 | 加入基线 | 加入日期 | operator | profile |
|---------|------|------|----------|----------|----------|---------|
| ai-rust | （待 M0 自注册时填） | — | — | — | 瑾瑜 | — |
| ai-frontend | （待 M0 自注册时填） | — | — | — | 瑾瑜 | — |
| ai-qa | （待 M0 自注册时填） | — | — | — | 瑾瑜 | — |
```

```bash
# 2. done commit（基线 BUMP）
cd G:\test1.x
git add -A
git commit -m "BL-20260812-02: baseline: M1 完成，进入 M0 准备"
```

**验证**：
```bash
git log --oneline
# 期望看到 3 条 commit：
# BL-20260812-02: baseline: M1 完成，进入 M0 准备
# BL-20260812-01: chore: tauri.conf 窗口配置 + 根 README + 代码风格配置
# BL-20260812-01: feat: Tauri 2.x + Svelte 5 工程脚手架
# BL-20260812-01: chore: Init chain-gui 工程 + ai_workspace 协作协议
```

### Step 6：写自验收报告

路径：`G:\test1.x\ai_workspace\ai-rust\self_review\M1.md`

```markdown
# M1 自验收报告 — 项目脚手架

code_baseline: BL-20260812-01 → BL-20260812-02
ai: ai-rust
date: 2026-MM-DD

## 任务清单完成度

- [x] Init 状态确认（Step 0）
- [x] 安装 Rust / Node / Tauri CLI（Step 1）
- [x] Tauri 工程初始化 + 提交（Step 2）
- [x] tauri.conf.json 窗口配置 + 根 README + 代码风格配置（Step 3）
- [x] cargo tauri dev 开窗验证（Step 4，截图：screenshots/M1/）
- [x] done commit + 基线 BUMP（Step 5，BL-20260812-01 → BL-20260812-02）
- [x] 自验收报告（本文件）

## 验证证据

- 截图：`ai_workspace/ai-rust/screenshots/M1/dev-window.png`
- git log：
  \`\`\`
  <hash1> BL-20260812-01: chore: Init chain-gui 工程 + ai_workspace 协作协议
  <hash2> BL-20260812-01: feat: Tauri 2.x + Svelte 5 工程脚手架
  <hash3> BL-20260812-01: chore: tauri.conf 窗口配置 + 根 README + 代码风格配置
  <hash4> BL-20260812-02: baseline: M1 完成，进入 M0 准备
  \`\`\`

## 环境版本（实测）

- rustc: <version>
- cargo: <version>
- node: <version>
- npm: <version>
- cargo tauri: <version>
- OS: Windows 11 10.0.26200

## 已知问题 / 留给 M0 的事项

- ai_workspace/ 下三个 AI 文件夹（ai-frontend / ai-qa）还是空骨架，等 M0 各自接入
- CODE_STATE "已加入 AI 名单" 区为空，等 M0 填充

## 交接给 coze

请 coze 验收后启动 M0（AI 集中接入）—— 写三个加入任务卡（ai_join_ai-rust.md / ai_join_ai-frontend.md / ai_join_ai-qa.md），让三个执行 AI 按 v3 §6.5 协议自注册。

—— ai-rust
```

## 验收标准（coze 视角）

收到 ai-rust 的自验收报告后，coze 做以下检查：

1. **路径存在性**：`G:\test1.x\` 存在，含 src-tauri/、src/、ai_workspace/、README.md、.gitignore
2. **ai_workspace 完整性**：
   - `ai_workspace/README.md` 含 `inherits: render_unified ai_workspace protocol`
   - `ai_workspace/CODE_STATE.md` 含 BL-20260812-01 和 BL-20260812-02 两条基线
   - 四个 AI 文件夹都存在（含 ai-coordinator），每个有 CURRENT.md
3. **git log**：4 条 commit（Init + 3 × M1），message 格式正确
4. **dev 模式可启动**：`cargo tauri dev` 能开窗（coze 验或 ai-rust 提供截图）
5. **自验收报告**：`ai_workspace/ai-rust/self_review/M1.md` 含完整 7 项勾选 + 截图 + git log + 环境版本

**全部通过 → coze 在 ai_workspace/ai-coordinator/reviews/M1.md 写验收通过记录 → 启动 M0**
**有任一不通过 → 写验收不通过记录 + 列出待修项 → 让 ai-rust 重做**

## 交接

完成后 ai-rust 通知用户（你），把以下内容转给 coze（我）：

1. `ai_workspace/ai-rust/self_review/M1.md` 的内容
2. `ai_workspace/ai-rust/screenshots/M1/` 的截图
3. 工程根目录的绝对路径（应是 `G:\test1.x\`，如有调整告知新路径）

coze 验收通过后会立即启动 M0（写三个加入任务卡，触发三个 AI 自注册）。

---

## 附录：工单来源与范围

- **规划书**：`/Coze/Drive/怀瑾的新项目（4）/chain_protocol_gui_impl_plan_v3.html` §M1 §6.4
- **本工单范围**：
  - ❌ git init + ai_workspace 目录结构（→ Init 任务卡）
  - ❌ CODE_STATE 初始台账（→ Init 任务卡）
  - ❌ 四个 AI 文件夹 CURRENT.md（→ Init 任务卡）
  - ✅ Init 状态确认（Step 0）
  - ✅ 装 Rust/Node/Tauri CLI（Step 1）
  - ✅ Tauri init + dev 开窗（Step 2-4）
  - ✅ 根 README + 代码风格配置（Step 3）
  - ✅ done commit + 基线 BUMP（Step 5）
  - ✅ 自验收报告（Step 6）

—— coze
2026-08-13 06:30

---

> 本内容由 Coze AI 生成，请遵循相关法律法规及《人工智能生成合成内容标识办法》使用与传播。
