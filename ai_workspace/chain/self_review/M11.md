# M11 自验报告 — AI 工程推进适配

> 主导 AI：chain · 基线 BL-20260814-01 · commit `98d9227` · 2026-08-14

## 一、改动全览（18 files +962/-36）

### 1. AI_GUIDE v2（`resources/AI_GUIDE.md`，+309 行）
- 首行新增版本标记 `<!-- CHAIN_GUIDE_VERSION: 2 -->`
- 恢复 9 条守则（第 9 条「不拍脑袋实现」此前在 339 行旧版中丢失）
- Chain Protocol 新增：`evidence` 可选字段（§2 字段表）、§4.7 过程留痕（试错记录 + PROCESS_LOG 约定）、§4.8 产物规范（artifacts/<task-id>/ 分层 + 引用优于复制）、§5 自查清单 3 项、§6 报错对照 1 项、§7 示例补充 evidence 用法

### 2. 后端（Rust）
| 文件 | 改动 |
|---|---|
| `commands/ai_guide.rs` | +AI_GUIDE_VERSION 常量 + parse_guide_version 解析 + get_guide_version 命令 + 5 条测试（含「标记与常量一致」同步测试） |
| `commands/init_chain.rs` | 指南写入从「存在即不写」改为**版本对比刷新**：无标记/旧版 → 刷新；同版/新版 → 保留（尊重批注）。+6 条测试覆盖 6 种场景 |
| `commands/process_log.rs`（新） | append_log（时间戳追加+表头自动建+多行合并）/ get_process_log + 4 条测试 |
| `scanner/walker.rs` | 扫描时检测指南陈旧 → warning 提示 + 2 条测试 |
| `scanner/validator.rs` | evidence 字段校验（可选，填了必须字符串数组）+ 测试 |
| `model/node.rs` + `model/mod.rs` | Node.evidence 字段（serde default，向后兼容）；UpdateFields.evidence |
| `commands/update_node.rs` | 透传 evidence + 1 条新测试 |
| `lib.rs` | 注册 3 个新命令 |

### 3. 前端（Svelte）
| 文件 | 改动 |
|---|---|
| `Sidebar.svelte` | 只读元信息行（rev/建于/更于/父节点）+ evidence 编辑框 + 过程日志追加块 |
| `StatusBar.svelte` | 指南版本徽标 + 过程日志抽屉 |
| `App.svelte` | 传递 chainDir + save 签名加 evidence |
| `types.ts` | ChainNode.evidence |
| `README.md` | 里程碑补全 M1-M11 |

### 4. 版本
- `tauri.conf.json`：1.1.2 → **1.2.0**

## 二、验证结果（全部实跑）

| 项 | 结果 |
|---|---|
| cargo test（单元） | **56 passed, 0 failed**（基线 40 → +16） |
| cargo test（集成） | 1 passed（scan_chain_test）+ real_dir_verify #[ignore] 预留 |
| 真实目录端到端 | `cargo test --test real_dir_verify -- --ignored` **1 passed**：init→幂等→update(evidence)→日志追加×2→指南 v1 刷新→同版保留，全链路在 `G:\deepseek\test\chain-gui-验证` 实测通过 |
| svelte-check | 0 errors / 0 warnings |
| vite build | 通过（502KB JS，chunk 警告为既有现象） |
| cargo build --release | 通过（仅 linker_messages 警告，既有） |

## 三、自检（对照守则）

- 守则 8 不跳验证：三轮全测 + 真实目录端到端 ✓
- 守则 9 不拍脑袋：指南协议增强全部来自本次渲染器实战的实证需求（旧指南丢失第 9 条的事实核查、证据脱钩、结论多处复制漂移）✓
- 透明性：真实目录验证中发现两处可改进点已如实记录（见下）

## 四、遗留与风险（如实报告）

1. **未做 GUI 实装验证**：本机无 Tauri GUI 运行验证（窗口交互/抽屉视觉），逻辑层已由测试覆盖，UI 视觉待开发者实装验收
2. **前端证据路径无存在性校验**：evidence 填了不存在的路径，软件只校验格式不校验文件存在——留作后续优化（扫描时检查并 warning）
3. **日志抽屉大文件性能**：PROCESS_LOG 全文一次读回，日志极长时需分页（当前场景足够）
4. **打包**：开发者执行 `cargo tauri build`，产物在 `src-tauri/target/release/bundle/nsis/`，预期 ~2.2MB（v1.1.0 为 2.16MB）
